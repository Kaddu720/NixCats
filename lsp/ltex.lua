return {
  cmd = { 'ltex-ls' },
  filetypes = { 'markdown', 'text', 'latex', 'tex' },
  root_markers = {},
  formatters = {
    ignoreComments = false,
  },
  settings = {
    ltex = {
      checkFrequency = "save",
    }
  }
}
