return {
  cmd = { "ltex-ls-plus" },
  filetypes = { "markdown", "text", "latex", "tex" },
  root_markers = {},
  formatters = {
    ignoreComments = false,
  },
  settings = {
    ltex = {
      checkFrequency = "save",
    },
  },
}
